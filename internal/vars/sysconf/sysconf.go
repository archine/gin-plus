package sysconf

import "github.com/archine/gin-plus/v4/component/config"

var (
	ProjectConfigure config.Configure // ProjectConfigure is the global configuration for the project, initialized in main.go
)
